global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ["alertname", "service", "severity"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default
    # Replace this with your pager/webhook receiver.
    # webhook_configs:
    #   - url: https://alerts.example.com/prometheus
    #     send_resolved: true
